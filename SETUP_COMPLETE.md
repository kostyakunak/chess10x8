# ✅ Миграция на Railway завершена!

## Что было сделано:

1. ✅ **Миграция базы данных применена** - таблицы созданы в Railway PostgreSQL
2. ✅ **Backend API настроен** - зависимости установлены, сервер протестирован
3. ✅ **Фронтенд обновлен** - код переведен на новый API

## 📝 Настройка для работы

### 1. Backend (server/.env)

Создайте файл `server/.env` со следующим содержимым:

```bash
DATABASE_URL=postgresql://postgres:caQqwIMLGgtXGZGqoeiTGyqtkKJipSsK@centerbeam.proxy.rlwy.net:33836/railway
PORT=3001
NODE_ENV=production
```

### 2. Frontend (.env в корне проекта)

Создайте файл `.env` в корне проекта:

```bash
# Для локальной разработки
VITE_API_URL=http://localhost:3001

# Для продакшена (после деплоя backend на Railway)
# VITE_API_URL=https://your-backend.railway.app
```

### 3. Запуск локально

**Backend:**
```bash
cd server
npm start
# или для разработки с автоперезагрузкой:
npm run dev
```

**Frontend:**
```bash
# В корне проекта
npm run dev
```

## 🚀 Деплой на Railway

### Backend:

1. В Railway создайте новый сервис "GitHub Repo"
2. Выберите ваш репозиторий
3. Установите **Root Directory**: `server`
4. Добавьте переменные окружения:
   - `DATABASE_URL` = `postgresql://postgres:caQqwIMLGgtXGZGqoeiTGyqtkKJipSsK@centerbeam.proxy.rlwy.net:33836/railway`
   - `NODE_ENV` = `production`
   - `PORT` - Railway установит автоматически
5. Railway автоматически задеплоит сервер

### Frontend (Netlify):

1. Откройте https://app.netlify.com/
2. Выберите ваш сайт **chess10x8**
3. **Site settings** → **Environment variables**
4. **Удалите старые переменные:**
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. **Добавьте новую переменную:**
   - `VITE_API_URL` = URL вашего Railway backend (например: `https://your-backend.railway.app`)
6. Перезадеплойте сайт

## ✅ Проверка

После деплоя проверьте:

1. Backend доступен: `https://your-backend.railway.app/health` должен вернуть `{"status":"ok"}`
2. Frontend подключается к backend (проверьте консоль браузера)
3. Игра создает комнаты и синхронизирует ходы

## 📊 Статус базы данных

База данных Railway настроена и готова к работе:
- ✅ Таблицы `game_rooms` и `game_moves` созданы
- ✅ Индексы созданы
- ✅ Триггеры для `updated_at` настроены

## 🔧 Устранение проблем

### Backend не запускается:
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных доступна

### Frontend не подключается:
- Проверьте `VITE_API_URL` в переменных окружения
- Убедитесь, что backend запущен и доступен
- Проверьте CORS настройки (должны быть настроены автоматически)

### WebSocket не работает:
- Убедитесь, что Railway поддерживает WebSocket (поддерживает)
- Проверьте, что URL использует правильный протокол (wss:// для HTTPS)

