# Миграция с Supabase на Railway PostgreSQL

Это руководство поможет вам перенести базу данных с Supabase на Railway PostgreSQL.

## 📋 Что было изменено

1. **Создан backend API** (`server/`) - Express.js сервер с WebSocket поддержкой
2. **Обновлен фронтенд** - теперь использует REST API вместо Supabase клиента
3. **Созданы миграции** - PostgreSQL миграции без Supabase-специфичных функций
4. **WebSocket для realtime** - заменен Supabase Realtime на WebSocket

## 🚀 Шаги миграции

### 1. Создание PostgreSQL базы на Railway

1. Зайдите на [Railway](https://railway.app/)
2. Создайте новый проект
3. Добавьте PostgreSQL сервис
4. Скопируйте `DATABASE_URL` из настроек базы данных

### 2. Применение миграций

Подключитесь к базе данных Railway и выполните миграцию:

```bash
# Используя psql
psql $DATABASE_URL -f railway/migrations/001_create_chess_tables.sql

# Или через Railway CLI
railway run psql -f railway/migrations/001_create_chess_tables.sql
```

Или выполните SQL напрямую в Railway Dashboard:
1. Откройте ваш PostgreSQL сервис
2. Перейдите в "Query" или "Data"
3. Скопируйте содержимое `railway/migrations/001_create_chess_tables.sql`
4. Выполните SQL

### 3. Настройка Backend API

1. Перейдите в папку `server/`:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
DATABASE_URL=postgresql://user:password@host:port/database
PORT=3001
NODE_ENV=production
```

4. Запустите сервер:
```bash
# Для разработки
npm run dev

# Для продакшена
npm start
```

### 4. Деплой Backend на Railway

1. В Railway проекте добавьте новый сервис "GitHub Repo"
2. Выберите ваш репозиторий
3. Установите Root Directory: `server`
4. Добавьте переменные окружения:
   - `DATABASE_URL` - из PostgreSQL сервиса
   - `PORT` - Railway автоматически установит
   - `NODE_ENV=production`

### 5. Обновление Frontend

1. Обновите `.env` файл в корне проекта:
```bash
# Удалите старые Supabase переменные
# VITE_SUPABASE_URL=...
# VITE_SUPABASE_ANON_KEY=...

# Добавьте новую переменную для API
VITE_API_URL=https://your-backend.railway.app
```

2. Для локальной разработки:
```bash
VITE_API_URL=http://localhost:3001
```

3. Пересоберите фронтенд:
```bash
npm run build
```

### 6. Обновление переменных окружения на Netlify (или другом хостинге)

1. Удалите старые переменные:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

2. Добавьте новую:
   - `VITE_API_URL` = URL вашего Railway backend

3. Передеплойте сайт

## 📁 Структура файлов

```
chess-10x8/
├── server/                 # Backend API
│   ├── index.js           # Express + WebSocket сервер
│   ├── package.json
│   └── .env.example
├── railway/
│   └── migrations/
│       └── 001_create_chess_tables.sql
├── src/
│   ├── lib/
│   │   ├── api.ts         # Новый API клиент (вместо supabase.ts)
│   │   └── supabase.ts    # Старый файл (можно удалить)
│   └── hooks/
│       └── useChessGame.ts # Обновлен для работы с новым API
└── RAILWAY_MIGRATION.md    # Этот файл
```

## 🔄 API Endpoints

Backend предоставляет следующие endpoints:

- `GET /api/rooms/:roomId` - Получить комнату
- `POST /api/rooms` - Создать новую комнату
- `PATCH /api/rooms/:roomId` - Обновить комнату
- `GET /api/rooms/:roomId/moves` - Получить ходы
- `POST /api/rooms/:roomId/moves` - Добавить ход
- `GET /health` - Health check

WebSocket подключение:
- `ws://your-backend.railway.app?roomId=xxx` - Подключение к комнате

## ⚠️ Важные замечания

1. **RLS политики удалены** - теперь безопасность обеспечивается на уровне API
2. **Realtime заменен на WebSocket** - работает аналогично Supabase Realtime
3. **Polling fallback** - если WebSocket не подключен, используется polling каждые 3 секунды
4. **Миграция данных** - если у вас есть данные в Supabase, их нужно экспортировать и импортировать в Railway

## 🐛 Решение проблем

### Backend не подключается к базе данных
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных доступна извне (Railway настраивает это автоматически)

### WebSocket не работает
- Проверьте, что WebSocket поддерживается на вашем хостинге
- Убедитесь, что URL использует правильный протокол (ws:// или wss://)
- Проверьте CORS настройки

### Frontend не может подключиться к API
- Проверьте `VITE_API_URL` в переменных окружения
- Убедитесь, что CORS настроен правильно на backend
- Проверьте, что backend запущен и доступен

## 📝 Следующие шаги

После успешной миграции вы можете:
1. Удалить старый файл `src/lib/supabase.ts`
2. Удалить зависимость `@supabase/supabase-js` из `package.json`
3. Удалить старые миграции Supabase из `supabase/migrations/`

