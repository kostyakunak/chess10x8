# ✅ Чеклист деплоя Backend на Railway

## 📋 Перед деплоем - проверьте:

- [x] Backend код в папке `server/`
- [x] `package.json` с зависимостями
- [x] Миграции применены к базе данных
- [x] DATABASE_URL известен

## 🚀 Шаги деплоя:

### 1. Откройте Railway
👉 https://railway.app/

### 2. Создайте новый проект
- Нажмите **"New Project"**
- Выберите **"Deploy from GitHub repo"**
- Выберите репозиторий: `kostyakunak/chess10x8`

### 3. Настройте сервис
После создания проекта Railway покажет ваш сервис. Нажмите на него и:

**Settings → Root Directory:**
```
server
```

**Settings → Start Command (должен быть автоматически):**
```
npm start
```

### 4. Добавьте переменные окружения
**Settings → Variables → New Variable:**

**Переменная 1:**
- Name: `DATABASE_URL`
- Value: `postgresql://postgres:caQqwIMLGgtXGZGqoeiTGyqtkKJipSsK@centerbeam.proxy.rlwy.net:33836/railway`

**Переменная 2:**
- Name: `NODE_ENV`
- Value: `production`

### 5. Дождитесь деплоя
Railway автоматически:
- Установит зависимости (`npm install`)
- Запустит сервер (`npm start`)
- Покажет логи

### 6. Получите URL
**Settings → Networking:**
- Нажмите **"Generate Domain"** (если еще нет)
- Скопируйте URL (например: `https://your-app.railway.app`)

### 7. Проверьте работу
Откройте в браузере:
```
https://your-app.railway.app/health
```

Должно вернуться: `{"status":"ok"}`

### 8. Обновите Netlify
После получения URL backend:

1. Откройте https://app.netlify.com/
2. Ваш сайт → **Site settings** → **Environment variables**
3. **Удалите:**
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. **Добавьте:**
   - Name: `VITE_API_URL`
   - Value: `https://your-app.railway.app` (URL из шага 6)
5. **Deploys** → **Trigger deploy** → **Deploy site**

## ✅ Готово!

После этого ваше приложение будет работать с Railway PostgreSQL!

## 🐛 Если что-то пошло не так:

**Backend не запускается:**
- Проверьте логи в Railway (Deployments → View Logs)
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что Root Directory = `server`

**Ошибка подключения к БД:**
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных Railway доступна

**Frontend не подключается:**
- Проверьте `VITE_API_URL` в Netlify
- Убедитесь, что backend доступен (проверьте `/health`)
- Проверьте CORS (должен быть настроен автоматически)

