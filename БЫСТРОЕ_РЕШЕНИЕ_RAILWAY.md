# ⚡ Быстрое решение для Railway

## Проблема: Railway не находит скрипт "start"

## ✅ Решение (выберите один из вариантов):

### Вариант 1: Изменить Start Command в Railway (САМЫЙ ПРОСТОЙ)

1. В Railway откройте ваш сервис
2. Перейдите в **Settings**
3. Найдите поле **"Start Command"** или **"Command"**
4. Измените на:
   ```
   cd server && npm start
   ```
5. Сохраните и подождите перезапуска

### Вариант 2: Использовать railway.json (уже создан)

Файл `railway.json` уже создан в корне проекта. Railway должен автоматически его подхватить при следующем деплое.

Если не помогло, используйте Вариант 1.

### Вариант 3: Добавлен скрипт в корневой package.json

Я добавил скрипт `"start": "cd server && npm start"` в корневой `package.json`.

Теперь Railway может запустить `npm start` из корня, и он автоматически перейдет в папку server.

## 🚀 После исправления:

1. Railway перезапустит сервис автоматически
2. Проверьте логи - должны увидеть "Server running on port..."
3. Проверьте `/health` endpoint

## 📝 Где найти Start Command в Railway:

1. Откройте ваш проект в Railway
2. Нажмите на **сервис** (не на проект)
3. Вверху справа нажмите **Settings** (шестеренка)
4. Прокрутите до секции **"Deploy"** или **"Build & Deploy"**
5. Найдите **"Start Command"** или **"Command"**

Если не видите - попробуйте найти вкладку **"Variables"** рядом, там может быть настройка команды запуска.

