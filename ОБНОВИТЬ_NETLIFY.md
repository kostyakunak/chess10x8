# 🔧 Обновление Netlify - КРИТИЧНО!

## Проблема:
Фронтенд пытается подключиться к `http://localhost:3001` вместо вашего Railway backend.

## ✅ Решение:

### 1. Получите URL вашего Railway backend

В Railway:
1. Откройте ваш сервис
2. **Settings** → **Networking**
3. Скопируйте URL (например: `https://your-app.railway.app`)

### 2. Обновите переменные окружения на Netlify

1. Откройте https://app.netlify.com/
2. Выберите ваш сайт **chess10x8**
3. **Site settings** → **Environment variables**

#### Удалите старые переменные:
- `VITE_SUPABASE_URL` - удалите
- `VITE_SUPABASE_ANON_KEY` - удалите

#### Добавьте новую переменную:
- **Name**: `VITE_API_URL`
- **Value**: `https://your-app.railway.app` (URL вашего Railway backend)

**ВАЖНО:** Используйте HTTPS URL, не HTTP!

### 3. Перезадеплойте сайт

После обновления переменных:
1. Перейдите в **Deploys**
2. Нажмите **Trigger deploy** → **Deploy site**
3. Или подождите автоматический редеплой (если включен)

### 4. Проверьте работу

После деплоя:
1. Обновите страницу сайта
2. Попробуйте создать новую комнату
3. Проверьте консоль браузера (F12) - не должно быть ошибок подключения

## ⚠️ Если не помогло:

Убедитесь, что:
- URL начинается с `https://` (не `http://`)
- Нет лишних слешей в конце URL
- Переменная называется именно `VITE_API_URL` (с префиксом VITE_)

