# 🔧 Решение проблемы с Root Directory в Railway

## Проблема
Railway не видит настройку Root Directory и пытается запустить `npm start` из корня проекта.

## Решение 1: Найти настройку Root Directory

В Railway настройка может называться по-другому:

1. Откройте ваш сервис в Railway
2. Нажмите на сервис (не на проект, а именно на сервис)
3. Перейдите в **Settings** (шестеренка вверху справа)
4. Прокрутите вниз до секции **"Build & Deploy"** или **"Deploy"**
5. Найдите поле **"Source"** или **"Root Directory"** или **"Working Directory"**
6. Установите: `server`

## Решение 2: Использовать railway.json (уже создан)

Я создал файл `railway.json` в корне проекта. Railway должен автоматически его подхватить.

**Если это не помогло**, попробуйте:

1. В настройках сервиса найдите **"Start Command"**
2. Измените на:
   ```
   cd server && npm start
   ```

## Решение 3: Создать package.json в корне (альтернатива)

Если ничего не помогает, можно создать `package.json` в корне, который запустит server:

```json
{
  "scripts": {
    "start": "cd server && npm start"
  }
}
```

Но лучше использовать Solution 1 или 2.

## Проверка

После настройки Railway должен:
1. Перейти в папку `server`
2. Установить зависимости (`npm install`)
3. Запустить сервер (`npm start`)

Проверьте логи - они должны показывать, что сервер запустился на порту 3001.

